{
  "name": "agent-spend-guard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Solana native subscriptions and allowances demo for bounded AI-agent spending.",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run demo"
  },
  "dependencies": {
    "@solana/kit": "^6.10.0",
    "@solana/subscriptions": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "engines": {
    "node": ">=22"
  }
}
